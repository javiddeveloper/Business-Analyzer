<div dir="rtl" style="text-align: right; font-family: Tahoma, Arial, sans-serif;">

# بررسی محصول و فنی — Coder Review
### از دو زاویه: صاحب کسب‌وکار / بیزینس، و CTO

> تاریخ بررسی: ۱۴۰۵/۰۶/۲۲ (2026-09-13) · نسخه‌ی کد: `master` @ `7211ce0` + تغییرات کامیت‌نشده‌ی working tree
> دامنه: کل مخزن (`server.js`، ۲۸ ماژول در `lib/`، داشبورد `public/admin.html`، ۲۰ فایل تست)

---

## ۰. خلاصه‌ی مدیریتی (TL;DR)

**این محصول واقعاً چیست؟** با اینکه اسمش «Coder Review» است، در عمل **دو محصول در یک باینری** است:

| | محصول A — ریویوی خودکار کد | محصول B — سنجش عملکرد تیم |
|---|---|---|
| کاربر | توسعه‌دهنده / تک‌لید | مدیر فنی / منابع انسانی |
| ورودی | MR + دیف + کد پروژه | جیرا + گیت‌لب + خروجی محصول A |
| خروجی | کامنت خطی روی MR، فایل `review/MR-<iid>.md` | امتیاز فرد، نمای تیم، خروجی اکسل |
| بلوغ | **بالا** — قابل استفاده در تولید | **متوسط** — قابل اتکا برای گفت‌وگو، نه برای تصمیم پرسنلی |
| ریسک | فنی (هزینه، پایداری موتور) | **سازمانی و حقوقی** (قضاوت درباره‌ی آدم‌ها) |

**قوت اصلی:** انضباط مهندسی این کد در حد بالایی است. صفر وابستگی npm، ۱۷۱ تست، و مهم‌تر از همه — **مستندسازی «چرا» در کنار «چه»**. تقریباً هر تصمیم عددی در `devScore.js` و `difficulty.js` با داده‌ی واقعی همان تیم کالیبره شده و دلیلش در کامنت نوشته شده. این چیزی است که در ۹۵٪ پروژه‌های داخلی دیده نمی‌شود و باعث می‌شود محصول B اصلاً قابل دفاع باشد.

**ضعف اصلی (و فوری‌ترین موضوع این گزارش):** یک تغییر کامیت‌نشده در working tree، **زنجیره‌ی اعتماد امتیازدهی را شکسته است** — از این پس خودِ مدل، اندازه و پیچیدگی MR توسعه‌دهنده را نمره می‌دهد، در حالی که کل معماری امتیاز بر این فرض بنا شده که این نمره را یک **maintainer انسانی** داده است. جزئیات در بخش ۳.۱. این یک باگ نیست؛ یک تغییر در قرارداد حکمرانی محصول است.

**سه اقدام فوری:**
1. مسدود کردن یا شفاف‌سازیِ ورود rating تولیدشده توسط AI به امتیاز فرد (بخش ۳.۱).
2. رفع تست شکست‌خورده که به `secrets.env` محلی وابسته است — یعنی الان **CI قابل اتکا وجود ندارد** (بخش ۳.۳).
3. تصمیم‌گیری درباره‌ی مدل استقرار: `claude-cli` روی لپ‌تاپ یک نفر ≠ سرویس سازمانی (بخش ۴.۱).

---

## ۱. نگاه صاحب کسب‌وکار — چه نیازی حل می‌شود و ارزشش چقدر است؟

### ۱.۱ نیازمندی‌هایی که واقعاً پشتیبانی می‌شوند

| # | نیاز کسب‌وکار | وضعیت | شواهد در کد |
|---|---|---|---|
| B1 | هر MR قبل از merge ریویو شود، حتی وقتی تک‌لید وقت ندارد | ✅ کامل | webhook + حالت polling + اجرای دستی |
| B2 | ریویو روی خط دقیق کد باشد نه یک متن کلی | ✅ کامل | `publish.js` — Discussion با `buildPosition` از پارس واقعی دیف |
| B3 | استانداردهای خودِ تیم در ریویو لحاظ شود | ✅ کامل | پایگاه دانش + `roles/tech-lead.md` |
| B4 | نشت اعتبارنامه/توکن در کد گرفته شود | ✅ کامل و **مستقل از مدل** | `checks.js` — الگوهای قطعی |
| B5 | خروجی ریویو در همان قرارداد فعلی تیم بماند | ✅ کامل | `review/MR-<iid>.md` |
| B6 | ابزار خودش چیزی را merge نکند | ✅ تصمیم درست | Approve بله، Merge هرگز — و approve فقط طبق ترتیب صف |
| B7 | مدیر بداند «امروز صبح سراغ چه کسی بروم» | ✅ خوب طراحی شده | نمای تیم، مرتب‌سازی روی «اولویت رسیدگی» نه امتیاز کل |
| B8 | خروجی قابل ارائه به بالادست | ✅ | خروجی XLSX بدون هیچ وابستگی (`xlsx.js`) |
| B9 | چند ریپازیتوری با یک داشبورد | ✅ | `projects.js` + سلکتور پروژه |
| B10 | جلوگیری از انفجار هزینه‌ی AI | ⚠️ نسبی | سقف ۲۴ دسته، سقف context — ولی **هیچ سقف ریالی/تعداد‌درخواستی وجود ندارد** |
| B11 | ابزار بدون تیم DevOps راه بیفتد | ✅ عالی | `npm start`، صفر dependency، تنظیمات از UI |
| B12 | استقرار سازمانی چندکاربره | ❌ **پشتیبانی نمی‌شود** | یک `ADMIN_TOKEN` مشترک، بدون کاربر/نقش/لاگ ممیزی |
| B13 | ارزیابی عملکرد قابل دفاع در جلسه | ⚠️ ریسک‌دار | بخش ۲ و ۳.۱ |

### ۱.۲ ارزش اقتصادی — کجا واقعاً پول در می‌آورد

بر اساس اعدادی که خودِ کد از داده‌ی واقعی این تیم ثبت کرده (۴ توسعه‌دهنده، میانه‌ی merge بین ۴٫۲ تا ۲۶٫۴ ساعت، ~۱۲ MR قابل بررسی در بازه):

- **صرفه‌جویی مستقیم:** اگر ریویوی انسانی هر MR ۳۰ تا ۹۰ دقیقه‌ی تک‌لید ببرد و ابزار نیمی از آن را جذب کند، روی ~۴۰ MR در ماه یعنی **۱۰ تا ۳۰ ساعت تک‌لید در ماه** — گران‌ترین ساعت تیم.
- **صرفه‌جویی غیرمستقیم (بزرگ‌تر):** `checks.js` نشت توکن را **قطعی** می‌گیرد. یک توکن گیت‌لب لو رفته در یک ریپوی خصوصی، هزینه‌اش با هیچ‌کدام از اعداد بالا قابل مقایسه نیست. این بخش به‌تنهایی ارزش نگه‌داشتن سرویس را دارد، حتی اگر مدل خاموش باشد.
- **جایی که پول می‌سوزد:** حالت خودکار روی هر push هر MR باز، با موتور HTTP، بدون سقف ریالی. یک روز شلوغ با ۱۰ MR فعال و ۵ push هر کدام = ۵۰ ریویو × تا ۲۴ فراخوانی = **تا ۱۲۰۰ فراخوانی مدل در یک روز**. هیچ جای کد این را نمی‌شمارد و هیچ‌جا هشدار نمی‌دهد.

### ۱.۳ نیازمندی‌هایی که پوشش داده نشده‌اند (شکاف بیزینسی)

| شکاف | چرا برای کسب‌وکار مهم است |
|---|---|
| **بدون داشبورد هزینه** | «این ماه چقدر برای ریویو خرج کردیم؟» بی‌جواب است. `usage.promptTokens/completionTokens` جمع می‌شود ولی هیچ‌جا تجمیع، ذخیره یا نمایش داده نمی‌شود. |
| **بدون سنجش اثربخشیِ خودِ ابزار** | هیچ عددی نمی‌گوید «از ۱۰۰ یافته‌ی این ماه، چندتا واقعاً درست بود». بدون این، ابزار در برابر اولین «AI حرف مفت زد» بی‌دفاع است. یک دکمه‌ی 👍/👎 روی هر یافته، ارزان‌ترین قابلیتی است که می‌شود اضافه کرد و بیشترین اعتبار را می‌خرد. |
| **بدون گزارش روند تیمی در زمان** | نمای تیم «الان» را می‌گوید. «کیفیت تیم نسبت به سه ماه پیش بهتر شده یا بدتر؟» — که سؤال واقعی مدیرعامل است — جواب ندارد (نمودار ماهانه فقط سطح فرد است). |
| **بدون SLA / هشدار** | اگر سرویس دو روز خاموش باشد، هیچ‌کس خبردار نمی‌شود. فقط `/health` هست و کسی آن را نمی‌خواند. |
| **وابستگی به یک نفر** | کل سرویس روی `PROJECT_PATH` یک ماشین مشخص و (در حالت پیش‌فرض) اشتراک شخصی `claude login` یک نفر می‌چرخد. این یک **ریسک bus-factor = 1** است، نه یک جزئیات فنی. |
| **بدون مسیر خروج داده** | داده در JSON فایلی است (خوب)، ولی هیچ export/backup خودکاری نیست و `data/` در `.gitignore` است. یک `rm -rf` یعنی از دست رفتن کل تاریخچه‌ی امتیاز و activity. |

---

## ۲. نگاه حاکمیتی — ارزیابی عملکرد انسان‌ها

این حساس‌ترین بخش محصول است و باید جدا قضاوت شود، چون خطا در آن هزینه‌ی سازمانی دارد نه فنی.

### ۲.۱ چه چیزهایی خوب انجام شده (و باید حفظ شود)

این‌ها تصمیم‌های نادری هستند که معمولاً در ابزارهای مشابه دیده نمی‌شود:

1. **مؤلفه‌ی بدون داده حذف می‌شود، صفر نمی‌گیرد.** کسی که زمان ثبت نمی‌کند پایین‌تر از کسی که بد ثبت می‌کند رتبه نمی‌گیرد. این دقیقاً همان جایی است که اکثر سیستم‌های امتیازدهی اعتبارشان را از دست می‌دهند.
2. **ضریب اطمینان `n/(n+5)`.** یک MR ریویوشده نمی‌تواند ۲۰٪ امتیاز کسی را تعیین کند.
3. **جریمه‌ی متقارن تخمین (`log2`).** بدون این، پد کردن تخمین پاداش می‌گرفت — یعنی سیستم به تیم یاد می‌داد دروغ بگوید.
4. **حذف «فعالیت اخیر» از امتیاز.** یک هفته مرخصی تأییدشده دیگر مثل افت مهندسی خوانده نمی‌شود. این یک تصمیم اخلاقی درست است.
5. **کاهش وزن «برنچ تک‌نویسنده» از ۳۰ به ۵.** استدلالش دقیقاً درست است: وقتی معیاری برای ۱۱ نفر از ۱۲ نفر رد می‌شود، دارد **روش کار تیم** را توصیف می‌کند نه عملکرد فرد.
6. **«نامشخص» به‌جای حدس.** جایی که داده ناقص است، صادقانه گفته می‌شود.
7. **تفکیک کامل روی صفحه.** هیچ‌کس مجبور نیست عدد را باور کند — می‌تواند بازش کند.
8. **سرعت merge عمداً امتیاز نمی‌گیرد** چون کسی را از هم جدا نمی‌کرد.

> این هشت مورد را به‌عنوان **قرارداد محصول** جایی مستند و قفل کنید. اولین باری که کسی بخواهد «امتیاز را ساده‌تر کنیم»، این‌ها همان چیزهایی هستند که اول قربانی می‌شوند و اعتماد تیم با آن‌ها می‌رود.

### ۲.۲ ریسک‌های باقی‌مانده در امتیازدهی

| ریسک | شرح | شدت |
|---|---|---|
| **قابل بازی‌دادن بودن `codeQuality`** | امتیاز از **آخرین** ریویوی هر MR ساخته می‌شود. یعنی «findings را رفع کن، دوباره ▶ بزن» درست است — ولی «MR را ببند، MR جدید کوچک باز کن» هم همین اثر را دارد و رفتار مطلوبی نیست. | متوسط |
| **`completion` مشوق تسک‌های کوچک است** | نسبت Done به کل. هرچه تسک ریزتر، امتیاز بهتر. با `mrSize` تا حدی خنثی می‌شود ولی صریحاً دیده نشده. | متوسط |
| **`onTime` وابسته به کیفیت Due date در جیرا** | اگر PM تاریخ‌ها را غیرواقعی بگذارد، ۲۵٪ امتیاز هر نفر تابع کیفیت کار PM است نه خودش. در کامنت کد اذعان شده که فقط ۶ تسک از ۸۷ سر موعد بوده‌اند — یعنی سیستم Due date این سازمان **خودش خراب است** و ۲۵٪ وزن روی یک سیگنال خراب نشسته. | **بالا** |
| **مقایسه‌ی بین‌فردی بدون نرمال‌سازی نقش** | یک نفر روی معماری کار می‌کند و یکی روی باگ‌فیکس؛ هر دو با یک فرمول سنجیده می‌شوند. ضریب پیچیدگی (±۱۲) تا حدی جبران می‌کند ولی سقفش کم است. | متوسط |
| **نبود «حالت فقط-مشاهده برای فرد»** | توسعه‌دهنده نمی‌تواند امتیاز خودش را ببیند و اعتراض کند. سیستمی که فقط مدیر می‌بیندش، به‌سرعت به «سیستم مچ‌گیری» تعبیر می‌شود. | **بالا (سازمانی)** |

**توصیه‌ی حکمرانی:** یک بند صریح در بالای صفحه‌ی امتیاز اضافه شود: «این عدد ورودی گفت‌وگوست، نه مبنای تصمیم پرسنلی» — و واقعاً هم همین‌طور استفاده شود. کد در `README` این را می‌گوید؛ **UI نمی‌گوید**، و کسی README را در جلسه باز نمی‌کند.

---

## ۳. نگاه CTO — یافته‌های فنی، به ترتیب اولویت

### 🔴 ۳.۱ [بحرانی] rating تولیدشده توسط AI وارد امتیاز انسان می‌شود

**وضعیت:** در تغییرات کامیت‌نشده‌ی working tree.

زنجیره‌ی اتفاق:

1. `lib/reviewer.js` و `lib/agentReview.js` (تغییر جدید) از مدل می‌خواهند `rating: {complexity, length}` برگرداند.
2. `lib/reportFile.js:352` آن را به‌صورت `C*** L****` در **آخرین خط** `review/MR-<iid>.md` می‌نویسد.
3. `lib/reviewSignoff.js:readReportBody` هنگام خواندن، **اول working tree را نگاه می‌کند** و فقط اگر فایل نبود سراغ گیت می‌رود.
4. `lib/difficulty.js:parseRating` آخرین خط را می‌خواند.
5. `lib/devScore.js:mrSize` (وزن ۱۵) و ضریب پیچیدگی (±۱۲ نمره) از همین می‌آید.

**نتیجه:** تا **۲۷ نمره** از امتیاز یک توسعه‌دهنده، از قضاوت مدلی می‌آید که همان MR را ریویو کرده — در حالی که کامنت‌های خودِ `devScore.js` و `difficulty.js` صریحاً می‌گویند این «قضاوت maintainer» است، و `reviewSignoff.js` یک ماژول کامل است که فقط برای همین ساخته شده که «فایل را خودِ ابزار نوشته ≠ کسی تأییدش کرده».

بدتر: چون `readReportBody` working tree را ترجیح می‌دهد، **rating تازه‌نوشته‌ی AI روی rating کامیت‌شده‌ی maintainer را می‌پوشاند** بدون اینکه هیچ‌کس متوجه شود. یعنی maintainer یک `C** L*` کامیت می‌کند، ابزار فردا دوباره اجرا می‌شود و روی دیسک `C**** L*****` می‌نویسد، و امتیاز فرد عوض می‌شود.

نکته‌ی دوم: `loadRatings` فقط چک می‌کند فایل **زمانی کامیت شده** (`signoffs.get(iid)`)، ولی برخلاف round-trip، **چک نمی‌کند که کامیت‌کننده maintainer بوده**. یعنی خودِ توسعه‌دهنده می‌تواند یک بار `review/MR-N.md` را کامیت کند و از آن به بعد rating آن MR را کنترل کند.

**پیشنهاد:**
- `rating` مدل را با یک تگ متمایز بنویسید (مثلاً `AI: C*** L****` روی خط جدا) و `parseRating` فقط فرمت بدون‌تگ (انسانی) را به امتیاز راه بدهد؛ نسخه‌ی AI فقط نمایش داده شود.
- یا `readReportBody` برای مسیر امتیازدهی، **همیشه از گیت** بخواند نه از working tree (نسخه‌ی روی دیسک همیشه محصول آخرین اجرای ابزار است).
- `loadRatings` را مثل round-trip به `isMaintainerAuthor` مقید کنید.
- تا زمان تصمیم‌گیری: این تغییر را کامیت نکنید، یا `mrSize` را موقتاً به `skip` اضافه کنید.

### 🔴 ۳.۲ [بحرانی] تست شکست‌خورده — و بدتر، تست وابسته به تنظیمات محلی

```
✖ review() covers far more than the old 8-batch cap in one run
  expected more than the old 8-batch cap, got 4 calls
  tests/reviewer-concurrency.test.js:76
```

علتش رگرسیون کد نیست: تست `budgetFor()` را صدا می‌زند که از `secrets.env` **واقعیِ همین ماشین** می‌خواند. با `AI_PROVIDER=9router` (تنظیم فعلی) پنجره ۳۲K است، `batchChars=36432`، و ۲۰ فایل در ۴ دسته جا می‌شوند نه ۲۰ تا.

اهمیتش فراتر از یک تست قرمز است: **مجموعه‌ی تست این پروژه قطعی (deterministic) نیست.** روی ماشین یک نفر سبز است و روی ماشین دیگری قرمز، فقط چون موتور فعالش فرق دارد. یعنی هر CI ای که راه بیندازید بی‌معنی خواهد بود.

**پیشنهاد:** تست‌ها با یک `secrets.env` ثابتِ fixture اجرا شوند (مثلاً `CR_SECRETS_FILE` یا override کردن `budgetFor` در تست)، و در `package.json` یک اسکریپت `test:ci` با env قفل‌شده اضافه شود.

### 🟠 ۳.۳ [مهم] تناقض مستندات با کد — سقف دسته‌ها

`README` در بخش قابلیت‌ها می‌گوید سقف ۲۴ دسته است (درست، `reviewer.js:47`)، ولی در بخش «محدودیت‌ها» می‌گوید «حداکثر ۸ دسته». همچنین می‌گوید «۵۵ تست» در حالی که ۱۷۱ تست وجود دارد. برای فایلی که تنها مستند محصول است و ۴۸ کیلوبایت طول دارد، این نوع واگرایی سریع انباشته می‌شود.

### 🟠 ۳.۴ [مهم] مدل امنیتی برای فراتر از localhost کافی نیست

`checkAdminAuth`: اگر `ADMIN_TOKEN` خالی باشد → هر درخواست از `127.0.0.1` مجاز است. این برای یک ابزار تک‌کاربره روی لپ‌تاپ درست است، ولی به‌محض استقرار روی سرور:

- **یک توکن مشترک برای همه** — نه کاربر، نه نقش، نه اینکه چه کسی چه کاری کرد.
- **بدون لاگ ممیزی** — «چه کسی Approve خودکار را روشن کرد؟» یا «چه کسی `secrets.env` را از UI عوض کرد؟» بی‌جواب است، در حالی که `/api/env` می‌تواند توکن گیت‌لب و کلید API را بازنویسی کند.
- **بدون rate limiting** روی `/api/review` — که هر فراخوانی‌اش پول خرج می‌کند.
- **`isLocal` پشت پروکسی فریب‌پذیر است** — اگر روزی پشت nginx برود، همه‌ی درخواست‌ها از `127.0.0.1` می‌آیند و کل API بدون احراز هویت باز می‌شود. این تله باید صریحاً در کد بسته شود (مثلاً اگر `ADMIN_TOKEN` خالی است، فقط روی `127.0.0.1` گوش بدهد: `server.listen(PORT, '127.0.0.1')`).
- **بدون مقایسه‌ی timing-safe** برای توکن‌ها (`===`). ریسک عملی پایین، ولی ارزان است که درست شود.

### 🟠 ۳.۵ [مهم] هیچ سقف هزینه یا حسابداری مصرفی وجود ندارد

`usage.promptTokens`/`completionTokens` در `stats` جمع می‌شود ولی هیچ‌جا **تجمیع، ذخیره یا نمایش** نمی‌شود. پیشنهاد حداقلی و ارزان:

- یک `data/usage.json` روزانه/ماهانه به تفکیک موتور و پروژه.
- یک سقف نرم (`MAX_REVIEWS_PER_DAY`) که وقتی رد شد، حالت خودکار خودش را خاموش کند و در داشبورد بگوید چرا.
- نمایش «مصرف این ماه» در هدر داشبورد.

### 🟡 ۳.۶ [متوسط] داشبورد ۱۸۲ کیلوبایتی و ۳۲۸۹ خطی در یک فایل

`public/admin.html` — حدود ۲۵۰۰ خط جاوااسکریپت inline در یک `<script>`. با فلسفه‌ی «بدون build step» سازگار است و برای رسیدن به اینجا انتخاب درستی بوده، ولی الان به آستانه رسیده:

- بدون تست UI، بدون قابلیت refactor امن.
- هر تغییر کوچک، کل فایل را در دیف قرمز می‌کند و **ریویوی خودِ این پروژه را غیرممکن می‌کند** (طنز تلخ: بزرگ‌ترین فایل پروژه، دقیقاً همان چیزی است که ابزار خودش L***** می‌دهد).

**پیشنهاد بدون شکستن فلسفه:** تقسیم به چند فایل استاتیک `public/js/*.js` که با `<script src>` معمولی بارگذاری شوند. صفر build step، صفر dependency، ولی قابل ریویو.

### 🟡 ۳.۷ [متوسط] ذخیره‌سازی فایلی — نقطه‌ی شکست بعدی

`cache.js` و `state.js` با `readFileSync`/`writeFileSync` روی JSON کار می‌کنند:

- **بدون نوشتن اتمیک** — قطع برق وسط `writeFileSync` روی `cache-dev-analytics-v2.json` (۲۴۴ کیلوبایت) یعنی فایل خراب. `readAll` آن را می‌بلعد و `{}` برمی‌گرداند، پس بی‌سروصدا کل کش پاک می‌شود (رفتار قابل قبول)، ولی همین اتفاق روی `reviewed.json` یعنی **ریویوی دوباره‌ی همه‌ی MRها و پست شدن دوباره‌ی همه‌ی کامنت‌ها**. راه‌حل: نوشتن روی فایل موقت + `rename` اتمیک. کم‌هزینه.
- **race در `cached()`** — کامنت «re-read» هست ولی بین read و write هنوز پنجره‌ی از دست رفتن نوشته‌ی همزمان وجود دارد. با ۳ ردیف موازی نمای تیم، عملاً پیش می‌آید.
- **بدون پاک‌سازی کش** — `data/` الان ~۱ مگابایت است با نسخه‌های v1/v2/v3 کنار هم (`cache-jira-assignee.json`, `-v2`, `-v3`). فایل‌های نسخه‌ی قدیمی هیچ‌وقت حذف نمی‌شوند.

### 🟡 ۳.۸ [متوسط] پایداری موتور و مسیر بازگشت

زنجیره‌ی fallback خوب طراحی شده (ایجنت → هر موتوری که کلید دارد → گزارش صریح شکست). دو نکته:

- `AGENT_TIMEOUT_MS = 15 دقیقه`. برای وب‌هوک هیچ مشکلی ندارد (پاسخ فوری داده می‌شود)، ولی یک MR بزرگ روی `claude-cli` می‌تواند ۱۵ دقیقه یک slot را اشغال کند و هیچ صف یا سقف همزمانی سراسری وجود ندارد — فقط سقف «یک ریویو در هر MR». ۱۰ MR همزمان = ۱۰ فرآیند `claude` روی همان ماشین.
- **بدون circuit breaker.** اگر موتور مدام ۵۰۰ بدهد، هر poll دوباره تلاش می‌کند. `fetchWithRetry` سه بار retry دارد، یعنی خرابی موتور به‌جای کاهش، **بار را سه برابر** می‌کند.

### 🟢 ۳.۹ [کم] موارد کوچک‌تر

- `secrets.env` روی دیسک plain-text است و `/api/env` می‌تواند آن را بازنویسی کند. برای ابزار محلی قابل قبول است؛ برای سرور مشترک نه.
- `mergeOrderBlockers` در `jobs.js` — `gitlab.listOpenMergeRequests()` بدون `projectId` صدا زده می‌شود و بعد در JS فیلتر می‌شود. با رشد تعداد پروژه‌ها، این یک فراخوانی سنگین بی‌مورد در هر approve است.
- `data/` در `.gitignore` است ولی هیچ مکانیزم backup ای وجود ندارد. `activity-reviews.json` و `ratings.json` **تنها نسخه‌ی موجود** تاریخچه‌ی امتیازند.
- ترکیب CRLF/LF (warning های گیت). یک `.gitattributes` مسئله را برای همیشه می‌بندد.
- README به `../business-generator-light` لینک می‌دهد — مسیر نسبی خارج از مخزن، برای هرکسی جز نویسنده بی‌معنی است.

---

## ۴. کدام آپشن‌ها روی «بررسی عملکرد» اثری ندارند

سؤال صریح شما. جدول زیر دقیقاً می‌گوید هر تنظیم/قابلیت، روی **امتیاز و ارزیابی توسعه‌دهنده** اثر دارد یا نه.

### ۴.۱ آپشن‌هایی که **هیچ اثری** روی ارزیابی عملکرد ندارند

این‌ها را می‌توانید آزادانه روشن/خاموش کنید بدون اینکه عدد کسی تکان بخورد:

| آپشن / تنظیم | چه می‌کند | چرا روی امتیاز اثر ندارد |
|---|---|---|
| **`inlineComments`** (کامنت خطی) | یافته را روی خط کد می‌گذارد یا ته صفحه | فقط شکل ارائه. `severityCounts` که به امتیاز می‌رود، قبل از این مرحله ساخته شده. |
| **`autoPost`** (ثبت خودکار کامنت روی گیت‌لب) | نتیجه را روی MR پست می‌کند یا فقط در داشبورد | `activity.recordReview` مستقل از پست شدن ثبت می‌شود. |
| **`autoApprove`** (Approve خودکار) | MR را approve می‌کند | Approve هیچ‌جا در `devScore.js` خوانده نمی‌شود. |
| **مرتب‌سازی طبق ترتیب merge** | فقط صف approve | خالص UI/فرآیند. |
| **`skipDrafts`** | Draft ها را خودکار ریویو نمی‌کند | ⚠️ **تقریباً** بی‌اثر — پایین را ببینید. |
| **`pollSeconds`** | فاصله‌ی چک کردن | فقط زمان‌بندی. |
| **دکمه‌ی «⏹ توقف»** | ریویوی در حال اجرا را لغو می‌کند | ریویوی لغوشده ثبت نمی‌شود. |
| **انتخاب موتور AI / مدل** | کدام مدل ریویو کند | ⚠️ **ظاهراً** بی‌اثر — پایین را ببینید. |
| **سقف context / `REVIEW_CONTEXT_TOKENS`** | چقدر کد به مدل می‌رسد | ⚠️ همان مورد بالا. |
| **پایگاه دانش** | استانداردها را به پرامپت اضافه می‌کند | ⚠️ همان مورد بالا. |
| **فیلتر بازه‌ی تاریخ در داشبورد** | فقط نمایش | محاسبه‌ی مجدد روی همان داده. |
| **خروجی XLSX** | صادرات | فقط خواندنی. |
| **`ADMIN_TOKEN` / تنظیمات دسترسی** | دسترسی به داشبورد | امنیت، نه سنجش. |
| **سلکتور پروژه‌ی فعال** | تب MRها کدام ریپو را نشان دهد | صفحه‌ی آنالیز **همیشه** همه‌ی پروژه‌ها را جمع می‌زند، مستقل از این سلکتور. |
| **webhook در برابر polling** | چطور ریویو trigger شود | مسیر یکسان، خروجی یکسان. |
| **`positives`** در خروجی مدل | نکات مثبت | هیچ‌جا امتیاز نمی‌دهد — فقط متن گزارش. |
| **رنگ پیل شاخه‌ی مقصد** | develop سبز / بقیه نارنجی | صرفاً بصری. هیچ امتیازی بابت شاخه‌ی درست داده نمی‌شود. |
| **سرعت merge (open→merge)** | نمایش داده می‌شود | **عمداً** امتیاز نمی‌گیرد — چون کسی را از هم جدا نمی‌کرد. |
| **«فعالیت اخیر» / recency** | برچسب وضعیت | **عمداً** از امتیاز حذف شده. |
| **Story Points در جیرا** | خوانده می‌شود | ۰٪ پر شده، پس عملاً هیچ اثری ندارد (ولی اگر تیم شروع کند، خودش وارد می‌شود). |
| **Priority در جیرا** | خوانده و نمایش داده می‌شود | تقریباً همه `Critical_SO` — قدرت تفکیک صفر. |

### ۴.۲ آپشن‌هایی که **به‌نظر بی‌اثرند ولی نیستند** ⚠️

این‌ها تله‌های واقعی‌اند و باید در تیم اعلام شوند:

| آپشن | اثر پنهان روی امتیاز |
|---|---|
| **انتخاب موتور AI** | `codeQuality` (وزن ۲۰) از تعداد و شدت یافته‌ها می‌آید. یک مدل سخت‌گیرتر = یافته‌ی بیشتر = امتیاز پایین‌تر برای **همان کد**. یعنی **عوض کردن مدل، امتیاز تیم را جابه‌جا می‌کند** بدون اینکه کسی کد بهتر یا بدتری نوشته باشد. امتیازها بین دو دوره با موتورهای مختلف قابل مقایسه نیستند. |
| **سقف context / اندازه‌ی پنجره** | context کمتر = بخشی از دیف اصلاً دیده نمی‌شود = یافته‌ی کمتر = `codeQuality` **بالاتر**. یعنی سرویسِ کور، به توسعه‌دهنده پاداش می‌دهد. |
| **حالت ایجنت در برابر مسیر دیف** | ایجنت کل پروژه را می‌بیند و ایرادهای بین‌فایلی را پیدا می‌کند که مسیر دیف هرگز نمی‌بیند. دو حالت، دو توزیع متفاوت یافته. |
| **پایگاه دانش** | هر استاندارد جدید = یافته‌های جدید = افت `codeQuality` کل تیم در دوره‌ی بعد. |
| **`skipDrafts`** | ریویو نشدن Draft ها یعنی آن MRها هیچ رکورد ریویویی ندارند؛ اگر بعداً از Draft خارج و merge شوند بدون ریویو، از `codeQuality` غایب می‌مانند و از `reportedMRs` (مخرج round-trip) هم بیرون می‌مانند. |
| **`rating` تولیدشده توسط AI** (تغییر جدید) | تا **۲۷ نمره**. بخش ۳.۱. |
| **کامیت کردن یا نکردن `review/MR-<iid>.md`** | این **بزرگ‌ترین اهرم غیرمنتظره** است: تا وقتی maintainer فایل را کامیت نکند، آن MR در `reportedMRs` نیست، پس نه round-trip می‌گیرد، نه rating. یعنی **انضباط کامیت‌کردن فایل ریویو توسط لید، مستقیماً روی امتیاز افراد اثر می‌گذارد** — و افراد هیچ کنترلی رویش ندارند. |
| **حذف فایل ریویو** | round-trip را پاک می‌کند (مثبت برای فرد) ولی rating را نگه می‌دارد. یعنی حذف فایل، خالص به نفع فرد است. |

### ۴.۳ آپشن‌هایی که **مستقیماً** روی امتیاز اثر دارند

برای کامل بودن تصویر: `WEIGHTS` در `devScore.js`، `QUALITY_HALF_LIFE=3`، `CONFIDENCE_K=5`، `LATE_ZERO_DAYS=30`، `LENGTH_SCORE`، `COMPLEXITY_WEIGHT`، و سقف ±۱۲ ضریب پیچیدگی. این‌ها **در کد hardcode اند و از UI قابل تغییر نیستند** — که تصمیم درستی است (پارامتر امتیاز نباید با یک کلیک عوض شود)، ولی باید مکتوب باشد که تغییرشان یک تصمیم سازمانی است نه یک تنظیم.

---

## ۵. نقشه‌ی راه پیشنهادی

### فاز ۰ — همین هفته (پاک‌سازی ریسک)
1. تصمیم درباره‌ی rating تولیدشده توسط AI؛ تا آن زمان کامیت نشود (بخش ۳.۱).
2. رفع تست شکست‌خورده + جدا کردن تست‌ها از `secrets.env` محلی + اسکریپت `test:ci` (بخش ۳.۲).
3. نوشتن اتمیک برای `state.js` و `cache.js` (بخش ۳.۷).
4. اصلاح تناقض‌های README (۸ در برابر ۲۴ دسته، ۵۵ در برابر ۱۷۱ تست).
5. `server.listen(PORT, '127.0.0.1')` وقتی `ADMIN_TOKEN` خالی است (بخش ۳.۴).

### فاز ۱ — این ماه (اعتماد و دفاع‌پذیری)
6. حسابداری مصرف + سقف روزانه + نمایش در هدر (بخش ۳.۵) — بیشترین ارزش بیزینسی به ازای کمترین کد.
7. 👍/👎 روی هر یافته + نرخ صحت در داشبورد. بدون این، هیچ عددی برای دفاع از ابزار ندارید.
8. لاگ ممیزی برای هر تغییر `secrets.env`/settings و هر approve.
9. یک بنر صریح روی صفحه‌ی امتیاز: «ورودی گفت‌وگو، نه مبنای تصمیم پرسنلی».
10. backup خودکار `data/` (یک zip روزانه کافی است).

### فاز ۲ — سه ماه (تبدیل ابزار شخصی به سرویس تیمی)
11. کاربر و نقش به‌جای توکن مشترک؛ حالت «فقط خودم» برای هر توسعه‌دهنده.
12. شکستن `admin.html` به چند فایل استاتیک (بخش ۳.۶).
13. صف سراسری ریویو با سقف همزمانی + circuit breaker روی موتور (بخش ۳.۸).
14. نمای روند تیمی در زمان (سؤال مدیرعامل).
15. کاهش وزن `onTime` یا اصلاح فرآیند Due date در جیرا — ۲۵٪ وزن روی یک سیگنال خرابِ سازمانی نشسته (بخش ۲.۲).

---

## ۶. جمع‌بندی

| محور | نمره | یادداشت |
|---|---|---|
| تناسب با نیاز واقعی تیم | ۹/۱۰ | دقیقاً همان قراردادی که تیم قبلاً دستی داشت را خودکار کرده — نه بیشتر، نه کمتر |
| کیفیت مهندسی کد | ۸/۱۰ | صفر وابستگی، ۱۷۱ تست، مستندسازی «چرا» در سطحی نادر |
| آمادگی برای تولید (تک‌کاربره) | ۸/۱۰ | آماده است |
| آمادگی برای تولید (سازمانی) | ۴/۱۰ | احراز هویت، ممیزی، سقف هزینه، backup — هیچ‌کدام نیست |
| دفاع‌پذیری سیستم امتیازدهی | ۶/۱۰ | طراحی اخلاقی‌اش عالی است؛ ولی rating تولیدشده توسط AI و اهرم «کامیت فایل ریویو» شکاف‌های جدی‌اند |
| قابلیت نگهداری | ۷/۱۰ | `lib/` تمیز است؛ `admin.html` به سقف رسیده |

**حرف آخر:** این پروژه به‌شکل غیرمعمولی خوب فکر شده — به‌خصوص در جاهایی که معمولاً فکر نمی‌شود (حذف مؤلفه‌ی بدون داده، جریمه‌ی متقارن تخمین، حذف recency از امتیاز). خطر اصلی‌اش فنی نیست: خطرش این است که یک ابزار خوبِ **کمک به ریویو**، بی‌سروصدا به یک سیستم **قضاوت درباره‌ی آدم‌ها** تبدیل شود، بدون اینکه زنجیره‌ی اعتمادش (چه کسی نمره داد؟ انسان یا مدل؟) به‌همان دقتی که بقیه‌ی کد نوشته شده، نگه داشته شود. بخش ۳.۱ نشان می‌دهد این اتفاق همین الان، در working tree، در حال رخ دادن است.

</div>
